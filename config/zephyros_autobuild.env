CONFIG_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
if [ -f "$CONFIG_DIR/openwrt_autobuild.env" ]; then
    # Load shared mail notification settings.
    . "$CONFIG_DIR/openwrt_autobuild.env"
fi

PKG_VERSION=0.0.0
MODEL_LINEUP=GDM7275X
ZEPHYROS_CONFIG_SELECT=7
ZEPHYROS_CONFIG_NAME=gdm7259x_nsa
ZEPHYROS_REPO_URL=https://jamesahn@vcs.gctsemi.com/OS/Zephyros
